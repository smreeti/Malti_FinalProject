<!DOCTYPE html>
<html lang="en">

<!-- HTML Header-->
<%- include('layouts/header') -%>

    <body>
        <!-- Navigation -->
        <%- include('layouts/navbar') -%>

            <!-- Page Header-->
            <header class="masthead" id="loginBanner">
                <div class="container position-relative px-4 px-lg-5">
                    <div class="row gx-4 gx-lg-5 justify-content-center">
                        <div class="col-md-10 col-lg-8 col-xl-7">
                            <div class="site-heading">
                                <h1>Manage Inventory</h1>
                                <span class="subheading">Welcome to Manage Page</span>
                            </div>
                        </div>
                    </div>
                </div>
            </header>

            <main>

                <% if(result !=null && result.length>0) { %>
                    <table class="selectedBookTable">
                        <thead>
                            <tr>
                                <th>S.N.</th>
                                <th>Order Number</th>
                                <th>Quantity</th>
                                <th>Ordered Date</th>
                                <th>Verify</th>
                                
                            </tr>
                        </thead>

                        <tbody>
                            <% for(let i=0; i< result.length; i ++){ %>
                                <tr>
                                    <td>
                                        <%= i+1 %>
                                    </td>
                                    <td>
                                        <%= result[i].orderNumber %>
                                    </td>
                                    <td>
                                        <%= result[i].quantity %>
                                    </td>
                                    <td>
                                        <%= result[i].orderedDate.toISOString().split('T')[0] %>
                                    </td>
                                    <td> <a href="/manageOrderItem/<%= result[i].bookOrderID %>"> See Details</a></td>
                                </tr>
                                <% } %>

                        </tbody>
                    </table>
                    <%}else{ %>
                        <p> No book orders yet!</p>
                        <%} %>
            </main>

            <!--Footer-->
            <%- include('layouts/footer') -%>
    </body>

</html>